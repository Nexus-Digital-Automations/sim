/**
 * Unified Copilot Components
 *
 * Exports the unified copilot system that can switch between
 * external copilot (Claude API) and local copilot (Parlant agents).
 */

export { UnifiedCopilot, type UnifiedCopilotRef } from './UnifiedCopilot'