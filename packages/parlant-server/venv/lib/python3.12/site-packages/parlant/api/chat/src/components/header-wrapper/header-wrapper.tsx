import type { ReactNode } from 'react'
import { twMerge } from 'tailwind-merge'

const HeaderWrapper = ({ children, className }: { children?: ReactNode; className?: string }) => {
  return (
    <div
      className={twMerge(
        'sticky top-0 z-10 flex h-[70px] min-h-[70px] justify-between rounded-ss-[16px] rounded-se-[16px] border-[#F3F5F9] bg-white',
        className
      )}
    >
      <div className='w-[12px] min-w-[12px]' />
      {children}
      <div className='w-[12px] min-w-[12px]' />
    </div>
  )
}

export default HeaderWrapper
